//Will hide the user's create password with hashing and salt so that it can be added to the DB
//Will verify the inputed PW in order to verify if they used the correct PW so that they can login to the specfic account they want

var bcrypt = require('bcryptjs');

const generatePassword = () => {

}

const verifyPassword = () => {

}